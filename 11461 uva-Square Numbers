#include <stdio.h>
#include <math.h>
using namespace std;

int main ()
{
    int a, b, c;
    while (scanf (" %d %d", &a, &b) == 2) {
        if (a == 0 && b == 0) break;
        b = sqrt (b);
        a--;
        a = sqrt (a);
        a = b - a;
        printf ("%d\n", a);
    }
    return 0;
}
